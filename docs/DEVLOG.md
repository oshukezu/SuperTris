# SuperTris 開發工作日誌 (Development Log)

---

### [2026-09-03] 1P/2P選單固定英文放大至21px、首頁底部24px大積木落位堆疊動畫
- **首頁選單優化**：
  - `1P player` 與 `2P player` 固定為英文（中英文模式均顯示純粹經典的 1P/2P 標籤）。
  - 按鈕字級提升放大至 **`21px`**（加粗），蘑菇游標加大至 `22px`，手機中央極致醒目大器。
- **首頁底部 24px 大積木落位堆疊動畫升級**：
  - 磚塊加大至 `24px × 24px`，帶有立體像素高光與深色邊框。
  - 多顆彩色像素方塊（紅、黃、藍、綠）以 3.5 秒為週期緩緩從空中自然飄落至地面上方平整排開，視覺清晰動感。

---

### [2026-09-03] [SET] 選單 RESTART 改為 MAIN MENU 直接跳回首頁選單
- **按鈕功能與標籤優化**：
  - 將 `[SET]` 抽屜選單中的重開按鈕更新為 `[MAIN MENU]`（主選單 / `data-i18n="main_menu"`）。
  - 點擊按鈕時，自動收合抽屜、清空下落狀態、離線房間並呼叫 `returnToTitle()` 平穩重置頂部 HUD 並切換回 1985 NES 首頁選單。

---

### [2026-09-03] 修復 GitHub Pages CSS 404 跑版（style.css 回歸根目錄）
- **根治 CSS 載入失敗無樣式白屏**：
  - 將 `style.css` 移回專案根目錄，`index.html` 引用恢復為 `<link rel="stylesheet" href="style.css" />`。
  - 徹底消除 GitHub Pages 子目錄路徑快取 404 問題，確保所有樣式 100% 穩定加載。
